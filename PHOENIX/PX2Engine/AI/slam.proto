syntax = "proto3";

package SlamProto;

// MessageID
enum MsgType
{
	CA_RESERVED = 0;
	CA_SEND_LIDAR = 11;	
}

enum Result
{
	RE_OK = 0;
}

message LidarData
{
	int32 Signal = 1;
	float Angle = 2;
	float Distance = 3;
}

message LidarDataFrame
{
	repeated LidarData Frames = 1;
}